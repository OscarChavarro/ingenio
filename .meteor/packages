accounts-facebook
accounts-password          # Usado para autenticar usuarios
blaze-html-templates       # Compile .html files into Meteor Blaze views
ecmascript                 # Enable ECMAScript2015+ syntax in app code
insecure                   # Allow all DB writes from clients (for prototyping)
iron:router                # Elemento fundamental del sistema
jquery                     # Helpful client-side library
meteor-base                # Packages every Meteor app needs to have
meteorhacks:inject-initial # Usado del lado del servidor para seleccionar dinámicamente un CSS a partir de la base de datos
meteorhacks:npm            # Usado para el envío de correos Mandrill / Ajax del lado del servidor
mongo                      # The database Meteor supports right now
session                    # Client-side reactive dictionary for your app
standard-minifiers         # Compilador uglify y simplificador de JavaScript para modo de producción
tracker                    # Meteor's client-side reactive programming library
twbs:bootstrap             # Usado en estilos CSS de Bootstrap
http


npm-container
cfs:standard-packages
cfs:filesystem
aldeed:simple-schema
aldeed:collection2
aldeed:autoform
aldeed:delete-button
